# CI/Testing Environment Variables Template
# 本文件用于本地运行 CI 验证测试（pnpm ci:verify）
# 
# 使用方法：
# 1. 复制此文件到 .env.local: cp env.ci.template .env.local
# 2. 填写实际的 Supabase credentials
# 3. 运行: pnpm ci:verify
#
# 注意：
# - 不要提交 .env.local 到 git（已在 .gitignore）
# - CI 环境中这些值从 GitHub Secrets 注入

# Supabase Configuration (必需)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url_here
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# Test Mode (CI 自动设置)
NEXT_PUBLIC_TEST_MODE=true

# Playwright Configuration (CI 自动设置)
PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000
CI=true
