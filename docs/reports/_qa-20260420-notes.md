# QA Live Notes — 2026-04-20 demo.getfansee.com

> Working scratch pad while running the release-review walkthrough. Final report
> goes to `docs/reports/ui-walkthrough-20260420-demo-release-review.md`.

## Account / Environment

- Site: https://demo.getfansee.com
- Browser: Chromium via cursor-ide-browser MCP
- PC viewport requested 1440x900 (real visible pane appears narrower ~928px in screenshots; layout still observable)
- Mobile viewport will use 390x844
- Test accounts:
  - Fan: test-fan@example.com / TestPassword123! → login OK
  - Creator: test-creator@example.com / TestPassword123! → pending
- Modals on first load: Age Verification ("Enter Site — I am 18+") + Cookie Preferences both shown together (front + back); dismissible.

## Issue Log (running)

### Severity legend

- P0 = release blocker (broken main path / payment risk / auth gap / data err / global 5xx / fake CTA)
- P1 = main path reachable but degraded (broken sub-flow / state mismatch / mobile main-path issue)
- P2 = non-blocking layout / copy / minor interaction
- P3 = polish / a11y / perf

### Findings

| ID    | Sev    | Page / Route                        | Viewport / Identity | Title                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Evidence                                                                                                                         |
| ----- | ------ | ----------------------------------- | ------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- |
| F-001 | P1     | `/auth?mode=login` (login finalize) | PC / fan            | Console error `[auth] Session sync failed; continuing with client session.` plus two `[syncSessionCookies] failed: 401 Invalid or expired token` debug entries. Server-side cookie sync is broken right after login; client falls back. Risk: SSR pages may render as logged-out, causing UI flicker / role drift.                                                                                                                                                                                          | console messages 1-5; `screenshots/2026-04-20/pc/preflight-02-auth-clean.png`                                                    |
| F-002 | P1     | `/home` PC                          | PC / fan            | For You / Following tab bar (with the purple glow) overlays the first feed card — the avatar circle is visibly clipped by the tab strip top edge. Reproduces the issue from prior screenshots.                                                                                                                                                                                                                                                                                                              | `screenshots/2026-04-20/pc/preflight-03-home-fan-pc.png`                                                                         |
| F-003 | P2     | `/home` PC                          | PC / fan            | Floating EXCLUSIVE chip + lock icon visible above the bottom tab nav (mid screen), drawing attention to a "below" section that is the next post's locked overlay — combined with the bottom nav this creates visual clutter and looks like it is bound to the bottom bar. Will reconfirm.                                                                                                                                                                                                                   | same                                                                                                                             |
| F-004 | P1     | `/me` PC                            | PC / fan            | Initial render shows top header with "Sign In" + "Join" links and an empty body for ~3 seconds; then the real Profile UI appears. Same root cause as F-001 (server side cookie sync 401). Confirms user-reported "切换页面时登录态弹出 Sign In/Join 再自动接入" (N5).                                                                                                                                                                                                                                       | console + sequential snapshots                                                                                                   |
| F-005 | P1     | `/me` PC                            | PC / fan            | Bio character counter "0/200" is rendered behind the bottom navigation bar — visible only as garbled "0 200" between Home/Search icons. Bottom nav doesn't push content padding correctly when content area exceeds viewport.                                                                                                                                                                                                                                                                               | `screenshots/2026-04-20/pc/me-profile-fan-pc-viewport.png`                                                                       |
| F-006 | **P0** | `/me/wallet` PC                     | PC / fan            | First ~5 seconds after navigation: header shows "Sign In / Join" buttons (logged-out chrome) and body shows tiny pill skeletons that look nothing like the wallet UI. Then the real wallet ($30 balance + transaction history) appears. Same root cause as F-001/F-004 (server cookie sync 401). This is the most user-visible manifestation: a logged-in fan navigating to their wallet sees a fully-logged-out shell for 5 seconds. Can cause perception of being signed out + drive accidental re-login. | `screenshots/2026-04-20/pc/wallet-fan-pc-after-wait.png` (post-recovery); first-render screenshot would have shown Sign In/Join. |
| F-007 | P1     | `/me/wallet` PC                     | PC / fan            | Bottom navigation overlaps recent transaction list — "Recharge 6 days ago / +$10.00 / Completed" row is partially hidden under the bottom nav. Same padding bug pattern as F-005.                                                                                                                                                                                                                                                                                                                           | `screenshots/2026-04-20/pc/wallet-fan-pc-after-wait.png`                                                                         |
| F-008 | P3     | `/me/wallet` PC                     | PC / fan            | Wallet balance card has an isolated empty card with just a credit-card icon at lower-left of the balance card; appears to be a placeholder or aborted button. Confusing affordance.                                                                                                                                                                                                                                                                                                                         | same                                                                                                                             |

### Severity recategorization

After F-006 confirmed across `/me`, `/me/wallet`: the SSR cookie-sync failure pattern (F-001 / F-004 / F-006) is **upgraded to P0** because authenticated users see a "logged-out" chrome on every navigation. This will likely also occur on `/creator/studio`, `/creator/upgrade/*`, etc. (to be verified next).

| F-009 | P2 | `/purchases` PC | PC / fan | Each unlocked PPV row shows creator name as the literal string "Creator" / "Creator" instead of the actual creator handle (e.g. `@sophiacreative`). Author identity is missing on a transactional history view. | `screenshots/2026-04-20/pc/purchases-fan-pc.png` |
| F-010 | P1 | `/notifications` PC | PC / fan | Skeleton (loading state) renders 4 stacked notification cards with avatar + 3 lines, but the actual page resolves to a single centered empty-state card "No notifications yet". Skeleton **does not match** the real DOM at any visual dimension. Reproduces user-reported N3. | `screenshots/2026-04-20/pc/notifications-fan-pc-loading.png` vs `notifications-fan-pc-loaded.png` |
| F-011 | P2 | `/notifications` PC | PC / fan | Snapshot shows `Summary` and `Categories` headings (level 3) but neither is visible in viewport screenshots — they may be in a hidden right rail at desktop widths. At this viewport empty-state card has plenty of unused vertical space, but Summary/Categories never render visually here. UX inconsistency. | snapshot vs screenshot diff |
| F-012 | P2 | `/creator/mock-creator-1` PC | PC / fan | Two Subscribe CTAs in the same viewport: a "+ Subscribe $9.99/mo" button below the bio AND a sticky bottom "Subscribe for $9.99/mo" overlay just above the bottom nav. Same primary action duplicated. Sticky bottom Subscribe also overlaps the first post thumbnail (same pattern flagged in round2). Round2 P1 not fixed. | `screenshots/2026-04-20/pc/creator-mock1-fan-pc.png` |
| F-013 | **P0** | `/posts/mock-post-1` (and any post detail) | PC / fan | **`GET /api/posts/mock-post-1` returns 500**. The post detail UI shows "Failed to load post" with a Go Back button. This blocks every comment / PPV unlock / share / inline interaction that requires resolving a single post. Reproduces user-reported N8 (clicking comment from /home navigates here and fails). | `screenshots/2026-04-20/pc/post-mock1-fan-pc.png`; network request line `/api/posts/mock-post-1 → 500` |
| F-014 | P1 | `/home` PC | PC / fan | **Comment count button (`18` next to first post) is a fake button on PC.** Click is registered (button enters `[active, focused]` state) but **nothing happens**: no comment drawer opens, no inline expansion, no navigation, no toast, no console error. User has no feedback that the action was received. The desired behaviour per user spec is "expand inline input + show top 3 hottest comments". On Mobile (per N8) the same button instead navigates to `/posts/[id]` which 500s (F-013). Two opposite incorrect behaviours across viewports. | post-click snapshot shows same URL `/home` and no new region/dialog; console messages contain only `[CursorBrowser] Native dialog overrides installed` warnings, no errors |
| F-015 | P1 | `/creator/upgrade/apply` PC (≤928px viewport) | PC / fan | Bottom mobile navigation overlaps the form's primary actions: **Cancel and Submit Application buttons are rendered behind the fixed Home/Search/Alerts/Profile bar**. `browser_click` on Submit refused with "Click target intercepted by Alerts nav link". Form is unsubmittable at this width — fan cannot apply for creator upgrade. Same root cause as F-005/F-007 (missing bottom safe-area padding for fixed nav). Note: full 1440px desktop may not show bottom nav, but any window narrower than the responsive breakpoint repros. | `screenshots/2026-04-20/pc/upgrade-apply-submit-blocked.png` — Submit Application label is literally visible underneath the bottom nav row |
| F-016 | **P0** | `/creator/upgrade/kyc` (any identity) | PC / fan | **KYC integration is completely broken.** `GET /api/kyc/status` returns **500** on page load (so we can't read existing KYC status). After clicking "Start Verification", `POST /api/kyc/session` returns **500** — the button briefly shows "Preparing…" then reverts to "Start Verification" with **no toast/error message** to the user. No redirect to Didit, no verification flow ever opens. This **completely blocks creator activation** (no verified KYC → no Stripe Connect onboarding → no payouts). Reproduces user-reported "KYC 没有接入成功". `.env.local` has Didit sandbox creds set, so this is not a missing-env issue but a server-side error. | `network_requests` (full log saved): two 500s on `/api/kyc/status` and `/api/kyc/session` |
| F-017 | **P0** | `/creator/upgrade/kyc` PC | PC / fan | Same SSR session bug as F-001/F-004/F-006: initial render shows "Sign In / Join" header chrome and the **"Start Verification" CTA is missing for ~4 seconds**. After hydration the button appears. Combined with F-016, even after the user waits the button does nothing. Worst-case combined experience: logged-in fan visits KYC, sees logged-out chrome with no CTA, finally CTA appears, clicks it, sees "Preparing…" forever, gets nothing. | sequential snapshots (e25 missing → e25 present after 4s wait) |
| F-018 | P2 | `/me` Log Out button | PC / fan | Logout actually **works**, but click → /auth redirect takes ~6 seconds with **no loading indicator on the Log Out button** during the wait. User is left clicking and wondering, then suddenly redirected. Reproduces user-reported "点击退出登陆要很久". Recommend disabling the button + spinner during signOut. Logout itself + RBAC redirect are correct. | timing observed across multiple runs |
| F-019 | PASS | `/admin`, `/me`, `/me/wallet`, `/creator/studio` while logged-out | PC / guest | RBAC redirects all four protected routes to `/auth?redirect=<original>`. Auth gating works correctly. | snapshots across guest sweep |
| F-020 | **P0** | `/creator/studio/post/new` → publish flow | PC / creator | Publishing a free post finishes (button transitions Publish → Publishing… → redirect to `/home`) **without any success toast or confirmation**. After redirect, navigating to `/creator/studio/post/list` still shows **Total Posts: 0 / Published: 0 / Premium: 0** for the same creator. Either the post was silently dropped or the post-list endpoint doesn't reflect new posts. Network log around the publish does not show a POST to `/api/posts` (only `_vercel/insights` and `/api/auth/bootstrap`). Either way, **publish flow is broken** end-to-end: creator has no proof their content went live. Combined with the user-reported "发帖页缺少关键输入区" round2 risk, this fails the core creator value prop. | `screenshots/2026-04-20/pc/studio-post-list-creator-pc.png` (Total Posts: 0 after publishing); network log saved |
| F-021 | P1 | `/creator/studio/post/list` PC | PC / creator | Same SSR session flicker — first render shows "Sign In / Join" header chrome before content loads. Same root cause as F-001/F-004/F-006/F-017. Plus bottom navigation overlaps the bottom stat tile ("Total Revenue" hidden behind Search/Create icons). | `screenshots/2026-04-20/pc/studio-post-list-creator-pc.png` |
| F-022 | P1 | `/creator/studio` PC | PC / creator | Bottom navigation overlaps "Total Views" / "PPV Sales" stat tiles. Recurring layout bug: every fixed-bottom-nav page lacks bottom-safe-area padding (`pb-20` or equivalent). | `screenshots/2026-04-20/pc/studio-creator-pc.png` |
| F-023 | P2 | `/home` mobile (390×844) | Mobile / fan | For You/Following tab bar still **clips the first post's avatar** at narrow widths — the SC avatar circle is half hidden behind the tab strip. Reproduces F-002 on mobile. Plus the EXCLUSIVE chip + "Unlock Exclusive Content" CTA on the locked second post are visible behind the bottom nav. | `screenshots/2026-04-20/mobile/home-fan-mobile-firstfold.png` |
| F-024 | P2 | `/home` mobile | Mobile / fan | The bottom nav `+ Create` icon is centered visually but its label sits at an awkward offset; the entire bottom nav has misaligned icon/label spacing on the Create button (icon overlaps the label baseline). | same |
| F-025 | P3 | All authenticated pages | PC / fan | Header shows separate "Notifications (F8)" and "Notifications alt+T" toast regions in the accessibility tree (two toaster instances mounted). Likely a duplicate `<Toaster>` mount in the layout. Causes screen reader duplication. | every snapshot output |

### Summary Counts (PC + Mobile sweep)

- P0 (release blocker): F-001, F-004, F-006, F-013, F-016, F-017, F-020 — **7 distinct release blockers**
- P1 (degraded main path): F-002, F-005, F-007, F-010, F-014, F-015, F-021, F-022 — 8 issues
- P2 (non-blocking layout/interaction): F-003, F-009, F-011, F-012, F-018, F-023, F-024 — 7 issues
- P3 (polish): F-008, F-025 — 2 issues
- PASS: F-019 (RBAC redirects)
