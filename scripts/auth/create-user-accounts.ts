#!/usr/bin/env tsx

// Compatibility entry point. The canonical script contains the project-ref
// guard and requires E2E_TEST_USER_PASSWORD instead of a committed password.
import "../create-test-users";
